export const MOCK_TOKEN_BALANCE = 1240;

export const MOCK_SPENDING = {
  thisWeek: 340,
  thisMonth: 1120,
  lastActionLabel: "2 saat önce",
};

// Cost per video generation (and per edit-prompt regeneration)
export const TOKEN_COST_PER_VIDEO = 8;

// Maximum products a user can select per generation run
export const PRODUCT_SELECTION_LIMIT = 10;

// Estimated minutes per video (shown in UI as "~X min")
export const ESTIMATED_MINUTES_PER_VIDEO = 2;

// Demo simulation delay per video in ms (compressed for prototype)
export const DEMO_VIDEO_GENERATION_DELAY_MS = 1200;

// Sample video URL used as mock output for all generated videos
export const SAMPLE_VIDEO =
  "https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerBlazes.mp4";
